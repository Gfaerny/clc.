name: clc.build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: aptgetready
      run: apt install gcc make
    - name: installdep
      run: ./install_dependency.sh 
    - name: make
      run: make
    
